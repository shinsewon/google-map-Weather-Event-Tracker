import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  flexCenter: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
  boxShadows: {
    dropShadow: '2px 4px 20px rgba(0, 0, 0, 0.1)',
  },
};

export { theme };
